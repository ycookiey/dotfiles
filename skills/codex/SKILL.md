---
name: codex
description: Codex CLIで実装・計画・修正を行う（書き込み可能）。
---

実行: `bash ~/.claude/skills/codex/run.sh <dir> "<request>" [plan_file]`

事後: `<project_directory>/.codex-summary.md` を報告。問題がありそうなら `git diff <file>` で確認。
